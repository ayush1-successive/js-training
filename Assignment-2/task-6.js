// Write a function expression that takes in a number and returns true if it's even and false if it's odd.

const is_even = function (num) {
  return num % 2 == 0;
};

console.log(is_even(16)); // true

console.log(is_even(23)); // false
